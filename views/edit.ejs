<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f8f9fa;
      color: #212529;
      margin: 0;
      padding: 0;
    }
  
    form {
      max-width: 600px;
      margin: 20px auto;
      padding: 20px;
      background-color: white;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
  
    label {
      font-weight: bold;
      display: block;
      margin: 10px 0 5px;
    }
  
    input[type="text"], 
    input[type="date"], 
    input[type="number"], 
    input[type="file"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border-radius: 5px;
      border: 1px solid #ccc;
    }
  
    input[type="text"]:focus, 
    input[type="date"]:focus, 
    input[type="number"]:focus {
      border-color: #007bff;
      outline: none;
    }
  
    button[type="submit"] {
      background-color: #28a745;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
    }
  
    button[type="submit"]:hover {
      background-color: #218838;
    }
  
    img {
      margin-top: 10px;
      border-radius: 5px;
    }
  </style>
  
<body>
    <form action="/updateData" method="post" enctype="multipart/form-data">
        <input type="text" name="id" value="<%= data.id %>" hidden>
        
        <label>Movie Name:</label>
        <input type="text" name="moviename" value="<%= data.moviename %>" required><br>
        
        <label>Release Date:</label>
        <input type="date" name="releasedate" value="<%= data.releasedate %>" required><br>
        
        <label>Rating:</label>
        <input type="number" name="rating" step="0.1" value="<%= data.rating %>" required><br>
        
        <label>Language:</label>
        <input type="text" name="language" value="<%= data.language %>" required><br>
        
        <label>Image:</label>
        <input type="file" name="image"><br>
        
        <!-- Display Current Image -->
        <img src="<%= data.image %>" style="width: 100px; height: auto;"><br>
        
        <button type="submit">Update Item</button>
    </form>
    
</body>
</html>